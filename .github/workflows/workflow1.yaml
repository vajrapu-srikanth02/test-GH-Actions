name: 'aks-cluster-bootstrap'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        run: echo ${{ env.myname }}